name: Hello world

on: [push]

jobs:
  say-hello:
    runs-on: ubuntu-latest
    container: alpine:latest
    steps:
      - name: Say hello
        run: echo "hello world"
      - name: Say Hi
        run: pwd
