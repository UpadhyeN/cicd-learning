name: Hello world

on: [push]

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: echo "hello world"
  say-Hi:
    runs-on: alpine-latest
    steps:
      - name: pwd
        run: pwd
